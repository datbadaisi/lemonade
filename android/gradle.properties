org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError --enable-native-access=ALL-UNNAMED
android.useAndroidX=true
# Use the current Kotlin Gradle Plugin; Flutter stable still validates KGP directly.
android.builtInKotlin=false
# Flutter 3.47's Gradle plugin still uses the legacy variant API.
android.newDsl=false
